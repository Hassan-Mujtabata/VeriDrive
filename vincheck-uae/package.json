{
  "name": "vincheck-uae",
  "private": true,
  "version": "1.0.0",
  "description": "Runs both the FastAPI backend and React frontend with a single command.",
  "scripts": {
    "install:backend": "pip install -r backend/requirements.txt",
    "install:frontend": "cd frontend && npm install",
    "install:all": "npm run install:backend && npm run install:frontend",
    "backend": "cd backend && py -m uvicorn main:app --host 127.0.0.1 --port 8000 --reload",
    "frontend": "cd frontend && npm run dev",
    "dev": "concurrently -n BACKEND,FRONTEND -c blue,green \"npm run backend\" \"npm run frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
